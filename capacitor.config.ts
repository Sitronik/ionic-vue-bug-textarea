import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-vue-bug-textarea',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
